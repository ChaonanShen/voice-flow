# 三天黑客松 AI 语音输入法方案

## 1. 产品定位

产品可以定义为：

> 一个 AI 语音输入法，帮助用户把自然口语快速转成可直接发送、符合场景、表达清晰的文本。

核心 slogan：

> 不用想怎么写，直接说出来。

更强调一点：

> 我们做的不是语音转文字，而是语音到表达。

## 2. 三天内不要做什么

不建议做：

| 功能 | 原因 |
|---|---|
| 自己训练大模型 | 三天不现实 |
| 微调开源大模型 | 数据、部署、效果不可控 |
| 真正系统级输入法 | 开发复杂，容易踩系统权限问题 |
| 完整跨 App 输入 | 三天风险太高 |
| 离线 ASR + 离线 LLM | 工程成本高 |
| 实时逐字流式识别 | 可做但不是最重要 |

## 3. 推荐技术路线

最稳方案：

```text
前端录音
→ ASR API 转写
→ 规则预处理
→ LLM 意图解析
→ LLM 场景化改写
→ 展示/复制/插入
```

也就是：

```text
语音识别靠现成 ASR
文本理解和改写靠通用 LLM
产品差异化靠 Prompt Engine + 编排层
```

## 4. 核心模块设计

### 模块 1：语音输入

功能：

- 点击开始录音
- 点击结束录音
- 显示录音状态
- 调用 ASR 得到原始文本

输出示例：

```text
原始转写：嗯我今天下午可能会晚到十分钟然后帮我跟老师说一下
```

### 模块 2：AI 清理

能力：

- 去口头禅
- 补标点
- 断句
- 修正语序
- 保留原意

输出示例：

```text
我今天下午可能会晚到十分钟，麻烦帮我跟老师说一下。
```

这是最核心功能，必须做。

### 模块 3：场景识别

用户可以直接说：

```text
我要写邮件，内容是我下午会晚到十分钟，让老师见谅。
```

系统识别：

```json
{
  "scene": "email",
  "content": "我下午会晚到十分钟，让老师见谅",
  "tone": "formal"
}
```

然后生成：

```text
老师您好，我今天下午可能会晚到十分钟，给您带来不便深感抱歉，感谢您的理解。
```

### 模块 4：语音命令

建议只支持高频命令：

| 语音命令 | 系统行为 |
|---|---|
| 改正式一点 | 转正式语气 |
| 短一点 | 压缩文本 |
| 变成要点 | 输出 bullet points |
| 写成邮件 | 邮件格式 |
| 写成微信 | 聊天语气 |
| 加个标题 | 自动生成标题 |
| 翻译成英文 | 输出英文 |
| 更礼貌一点 | 增强礼貌表达 |

### 模块 5：多版本对比

同一句话展示多个版本：

| 版本 | 示例 |
|---|---|
| 原始转写 | 嗯我今天下午可能会晚到十分钟然后帮我说一下 |
| 清理版 | 我今天下午可能会晚到十分钟，请帮我说一下。 |
| 邮件版 | 老师您好，我今天下午可能会晚到十分钟，给您带来不便深感抱歉。 |
| 微信版 | 我下午可能会晚到十分钟，不好意思哈。 |
| 要点版 | - 下午可能晚到 10 分钟<br>- 需要提前告知老师 |

这个展示效果非常亮眼。

## 5. AI 编排层设计

你的核心创新可以叫：

> Voice Writing Pipeline

流程：

```text
Step 1：ASR 原始转写
Step 2：去除明显口头禅
Step 3：LLM 解析场景和命令
Step 4：注入用户词典和上下文
Step 5：LLM 生成目标文本
Step 6：检查数字、人名、专有名词是否保留
Step 7：输出多版本结果
```

## 6. Prompt 设计方式

### 第一次 LLM：意图解析

输入：

```text
用户语音文本：我要写邮件，内容是我下午晚到十分钟，让老师见谅，语气正式一点。
```

输出：

```json
{
  "scene": "email",
  "commands": ["formal", "polite"],
  "content": "我下午晚到十分钟，让老师见谅",
  "target": "polished_text"
}
```

### 第二次 LLM：生成文本

输入：

```text
场景：email
语气：formal, polite
内容：我下午晚到十分钟，让老师见谅
要求：保留原意，补全礼貌表达，不要编造原因。
```

输出：

```text
老师您好，我今天下午可能会晚到十分钟，给您带来不便深感抱歉，感谢您的理解。
```

## 7. 可以借鉴 Wispr Flow 的功能

| Wispr Flow 思路 | 你的三天版本 |
|---|---|
| Context-aware ASR | 读取当前输入框前 100 字作为上下文 |
| Personalized ASR/LLM | 本地用户词典 |
| Auto Cleanup | 清理强度：轻度/中度/重写 |
| Command Mode | 支持 8 个语音命令 |
| Multi-step pipeline | 意图解析 + 文本生成两步 |
| Low latency | 先显示原始转写，再异步润色 |
| Cross-app typing | 先做复制/粘贴，不做真系统输入法 |
| Token-level formatting | 用 Prompt 控制标点、断句、格式 |

## 8. 三天开发计划

### Day 1：跑通主链路

必须完成：

- 录音
- ASR 转写
- 原始文本展示
- AI 清理
- 一键复制
- 基础 UI

### Day 2：做差异化

完成：

- 场景识别
- 邮件/微信/笔记/会议纪要模式
- 语音命令
- 多版本输出
- 用户词典

### Day 3：包装体验

完成：

- 清理强度选择
- 修改痕迹展示
- 延迟统计
- Demo 样例
- 答辩页面
- 漂亮 UI

## 9. 最推荐的 Demo

用户说：

```text
我要写邮件，内容是我下午可能因为地铁晚点会晚到十分钟，让老师见谅，语气正式一点。
```

产品展示：

```text
识别场景：邮件
识别命令：正式一点、礼貌一点
提取内容：下午晚到十分钟，让老师见谅
```

输出：

```text
老师您好，我今天下午可能会因地铁晚点而晚到约十分钟，给您带来不便深感抱歉，感谢您的理解。
```

再展示对比：

```text
原始转写 → 清理版 → 邮件版 → 微信版 → 要点版
```

## 10. demo展示

你可以这么说：

> 我们没有重新训练大模型，而是构建了面向语音输入的 AI 编排层。ASR 负责听清楚用户说了什么，LLM 负责理解意图和改写表达，产品层通过场景模板、语音命令、用户词典和上下文注入，让自然口语变成可直接发送的文本。

最后一句：

> 我们做的不是语音转文字，而是语音到表达。
